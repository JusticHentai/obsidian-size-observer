import DataItem from '../types/DataItem'

const reloadPath = (curr: DataItem, prevList: DataItem[]): DataItem => {
  // prevList.push(curr)
  // let prev
  // for (const path of prevList) {
  //   if (path)
  // }
}

export default reloadPath
